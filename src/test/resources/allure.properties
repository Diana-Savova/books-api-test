# Picked up by the allure-java runtime if not overridden via -D system properties.
# The Surefire plugin sets this explicitly, so this is mostly a safety net for
# anyone who runs the tests outside Maven (e.g. straight from an IDE).
allure.results.directory=target/allure-results